# Frontend Configuration

# Socket.io server URL (auto-detects if not set)
# For development, leave blank to auto-detect from window.location.hostname
# For production, set to your backend URL
VITE_SOCKET_URL=

# Environment
VITE_NODE_ENV=development

# Enable debug logging
VITE_DEBUG=true
